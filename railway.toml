[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# 健康检查配置
[deploy.healthcheck]
path = "/api/health"
timeout = 300
interval = 15
